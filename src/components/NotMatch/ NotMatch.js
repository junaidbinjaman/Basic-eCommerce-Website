import React from "react";
import "./NotMatch.css";

const NotMatch = () => {
  return (
    <div>
      <h1 className="not-match">Sorry. Nothing Found</h1>
    </div>
  );
};

export default NotMatch;
